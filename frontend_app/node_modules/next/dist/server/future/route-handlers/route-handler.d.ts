import { BaseNextRequest, BaseNextResponse } from 'next/dist/server/base-http';
import { RouteMatch } from 'next/dist/server/future/route-matches/route-match';
export interface RouteHandler<M extends RouteMatch = RouteMatch> {
    handle(match: M, req: BaseNextRequest, res: BaseNextResponse, context?: any, bubbleResult?: boolean): Promise<any>;
}
