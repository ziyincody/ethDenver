import { BaseNextRequest, BaseNextResponse } from 'next/dist/server/base-http';
import { RouteKind } from 'next/dist/server/future/route-kind';
import { RouteMatch } from 'next/dist/server/future/route-matches/route-match';
import { RouteDefinition } from 'next/dist/server/future/route-definitions/route-definition';
import { RouteHandler } from 'next/dist/server/future/route-handlers/route-handler';
export declare class RouteHandlerManager {
    private readonly handlers;
    set<K extends RouteKind, D extends RouteDefinition<K>, M extends RouteMatch<D>, H extends RouteHandler<M>>(kind: K, handler: H): void;
    handle(match: RouteMatch, req: BaseNextRequest, res: BaseNextResponse, context?: any, bubbleResult?: boolean): Promise<boolean>;
}
