import { LocaleRouteDefinition } from 'next/dist/server/future/route-definitions/locale-route-definition';
import { RouteMatch } from 'next/dist/server/future/route-matches/route-match';
export interface LocaleRouteMatch<R extends LocaleRouteDefinition> extends RouteMatch<R> {
    readonly detectedLocale?: string;
}
